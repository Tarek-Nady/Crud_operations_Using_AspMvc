@model IEnumerable<IdentityRole>

@{
    ViewData["Title"] = "Index";
}

<h1>All Users</h1>
<br />
<br />

<form asp-action="Index">
    <div class="row align-items-center justify-content-center">
        <div class="col-8">
            <input type="text" placeholder="Search By Name" class="form-control" name="SearchValue" />
        </div>
        <div class="col-4">
            <input type="submit" value="Search" class="btn btn-success" />
        </div>
    </div>
</form>

<a asp-action="Create" class="mt-2 btn btn-primary"><i class="fas fa-add"></i> Create New Department</a>
<br />

@if(@TempData["Message"]!=null)
{
    <div class="alert alert-success my-3">
        @TempData["Message"]
    </div>

}



@if (Model.Count() > 0)
{
    <table class="table mt-4 table-striped table-hover">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(R=>R.Id )</th>
                <th>@Html.DisplayNameFor(R=>R.Name )</th>
                
                <th>Datails</th>
                <th>Update</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var Role in Model)
            {
                <tr>
                    <td>@Role.Id</td>
                    <td>@Role.Name</td>
                   
                    <partial name="_ButtonsPartailView" model="@Role.Id" />

                </tr>

            }
        </tbody>
    </table>
}
else
{
    <div class="mt-5 alert alert-warning">
        <h3>There is No Roles</h3>
    </div>
}
